import { LogType } from './../types/types';
export declare function parseTagsAsString(tags: Record<string, string>): string;
export declare function colorText(logType: LogType, message: string): string;
