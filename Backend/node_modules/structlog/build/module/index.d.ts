import { _InternalCaller } from './lib/caller';
import { LogOptions, LogTags } from './types/types';
export declare class StructuredLogger {
    #private;
    config: LogOptions;
    caller: _InternalCaller;
    constructor(options?: Partial<LogOptions>);
    debug(message: string, tags?: LogTags): void;
    info(message: string, tags?: LogTags): void;
    log(message: string, tags?: LogTags): void;
    error(message: string, tags?: LogTags): void;
    warn(message: string, tags?: LogTags): void;
}
export declare const logger: StructuredLogger;
